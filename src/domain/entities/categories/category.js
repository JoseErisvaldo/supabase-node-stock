class Category {
  constructor({ id, name, description, color, user_id }) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.color = color;
    this.user_id = user_id;
  }
}

export default CreateCategory;
